export const DECK_LIMITS = Object.freeze({
  NAME_MAX: 100,
  DESCRIPTION_MAX: 500,
  CARD_CONTENT_MAX: 5000,
  IPA_MAX: 100,
  EXAMPLE_MAX: 500,
  TAG_MAX: 50,
  TAG_COUNT_MAX: 20,
  BULK_FROM_VOCAB_MAX: 50,
  DECK_LIST_PAGE_SIZE_MAX: 50,
  CARD_LIST_PAGE_SIZE_MAX: 100,
  FOLDER_NAME_MAX: 100,
  FOLDER_LIST_PAGE_SIZE_MAX: 50,
});

export const DECK_VISIBILITY = Object.freeze({
  PRIVATE: "private",
  PUBLIC: "public",
});

export const CARD_STUDY_MODE = Object.freeze({
  FLIP: "flip",
});
